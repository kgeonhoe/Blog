---
title: SP에 몰아넣던 고객 분석 마트, dbt와 Dagster로 옮기기
categories:
  - "[[Projects]]"
  - "[[Data Engineering]]"
status: completed
stack:
  - "[[dbt]]"
  - "[[Dagster]]"
  - "[[MSSQL]]"
repo:
created: 2026-08-13
topics:
  - "[[Data Engineering]]"
  - "[[Data Pipelines]]"
tags:
  - project
  - dbt
  - dagster
  - data-mart
draft: true
---

> [!warning] 발행 전 확인
> - 회사·제휴사·상품명, 내부 시스템명은 일반화해서 썼습니다. 원본 코드를 추가로 인용할 때는 헤더 주석의 작성자 실명부터 제거할 것
> - 캠페인 운영 월(`campmonth` 값)은 예시 월로 치환된 상태 유지
> - 프로젝트 기간·역할(단독/팀) 한 줄 추가
> - 있으면 추가: 이관 전후 운영 지표(주간 추출 소요, 실패 시 복구 시간), dbt 선택 시 검토했던 대안, dbt docs DAG 또는 Dagster asset 그래프 스크린샷 1장
> - 이 콜아웃은 발행 시 삭제

보험사 콜센터(TM) 캠페인에 쓸 고객 리스트를 매주 뽑아 배분하는 일을 맡고 있었습니다. 캠페인마다 추출 조건과 배분 전략이 달랐고 그 로직 전부가 Stored Procedure 2~3개 안에 들어 있었습니다. 프로시저는 `#temp` 테이블을 단계마다 만들어 다음 단계로 넘겼습니다. 이 구조에서는 아래 3가지가 문제 였습니다. 

1. 버전 관리가 안 됐습니다. SP 안에 다양한 성격의 테이블들이 들어있어, 테이블 독립적으로 변경 사항 추적이 힘들었습니다. 
2. 어떤 테이블이 어떤 테이블에 기대는지는 코드를 끝까지 읽어야 알 수 있었습니다.  
3. 실행이 중간에 멈추면 어느 단계에서 멈췄는지 찾는 데만 시간이 갔습니다. 매주 추출할 때마다 이 비용을 그대로 치렀습니다.

그래서 프로시저 안의 단계들을 dbt 모델로 하나씩 물리화하고 실행은 Dagster에 맡기기로 했습니다.
결과물은 dbt 모델 64개와 매크로 2개, 그리고 그 모델들을 asset으로 읽어 실행하는 Dagster 프로젝트입니다.

| 항목  | 내용                                                                       |
| --- | ------------------------------------------------------------------------ |
| 배경  | 보험사 TM 캠페인 고객 리스트 주간 추출·배분                                               |
| 문제  | SP 2~3개에 로직 집중 → 버전 관리·의존성 파악·실패 지점 식별 불가                                |
| 스택  | dbt (dbt-sqlserver) · Dagster · MSSQL                                    |
| 규모  | 모델 64개(부품 42 / 조립 18 / 기타 4), `ref()`/`source()` 참조 111곳, 최장 직렬 체인 모델 9개 |
| 환경  | 폐쇄망 가상 윈도우. 도커·리눅스 VM·인터넷 불가                                             |

## dbt 사용이유

프로시저 안의 단계마다 이름 있는 산출물이 있고, 산출물 사이의 의존성이 코드에서 저절로 뽑히기를 바랐습니다. dbt는 SQL 파일 하나가 모델 하나(테이블 또는 뷰)이고 다른 모델을 `ref()`로 부르면 그 호출이 곧 의존성 그래프가 됩니다. 프로시저의 `SELECT ... INTO #temp` 한 단계를 `SELECT` 하나짜리 파일로 옮기면 그대로 모델이 되니 이관 자체는 기계적으로 진행할 수 있었습니다. 모델이 파일이니 버전 관리는 따라옵니다. 실행이 멈추면 dbt는 실패한 모델 이름을 로그에 남기고 `--select`로 그 모델부터 다시 돌릴 수 있습니다. 애초의 문제 세 가지가 도구 선택만으로 상당 부분 풀렸습니다.

저장소는 기존 운영 DB인 MSSQL 그대로였고 어댑터는 `dbt-sqlserver`를 썼습니다. 뒤에 쓰겠지만 실행 환경이 폐쇄망 가상 윈도우 한 대뿐이라 별도 서버나 컨테이너가 필요한 ETL 도구는 애초에 후보가 아니었습니다. dbt는 파이썬 패키지 하나로 설치되니 이 제약에 걸리지 않았습니다.

## 프로시저를 어떻게 쪼갰나

모델은 두 층으로 나눴습니다. 부품 모델 42개는 원장·캠페인 DB에서 고객 단위 속성을 집계합니다. 조립 모델 18개는 그 부품을 조립해 고객 마스터와 캠페인 타겟을 만듭니다. 폴더 기준의 구분입니다. 부품 폴더 안에 원장 추출·집계·최종 산출물 성격이 섞여 있었다는 문제는 마지막에 다룹니다. 의존 방향은 부품 → 조립 한 방향입니다. 프로시저의 단계 실행 순서는 모델 직렬 체인으로 그대로 옮겨졌고 가장 긴 체인은 모델 9개가 직렬로 이어집니다.

```mermaid
graph LR
  E["콜 에러 정제"] --> RP["최근 증권"]
  RP --> BASE["고객·증권 베이스"]
  BASE --> DATA["고객·증권 데이터"]
  DATA --> G["고객 구분"]
  G --> GRP["그룹 집계"]
  GRP --> SEG["고객·증권 세그먼트"]
```

위 그림은 캠페인 대상 세그먼트가 만들어지는 순서 중 하나를 단계 몇 개만 남기고 그린 것입니다. 왼쪽부터 읽으면 이렇습니다. 콜센터 통화 결과에서 잘못 기록된 건을 걸러내고 고객별로 가장 최근 증권번호를 뽑습니다.(고객이 해당 증권에 가입할 때 넣었던 전화번호 등이 가장 최신일 가능성이 높으므로..) 거기에 계약·특약·동의·블랙리스트 같은 원장 정보를 붙여 증권 한 건이 한 행인 넓은 테이블을 만듭니다. 이게 가운데의 "고객·증권 베이스"이고 SQL만 201줄입니다. 그 뒤 단계는 전부 이 테이블에서 시작합니다. 고객을 조건별로 나누고 나눈 결과를 묶어 최종 세그먼트 테이블까지 이어집니다.

이 베이스 테이블을 직접 읽는 모델이 8개입니다. 프로젝트에서 여러 곳에 재사용되는 테이블은 사실상 이것 하나이고 나머지 부품 모델 대부분은 딱 한 군데서만 쓰입니다. 그래서 베이스의 컬럼 하나가 바뀌면 뒤의 모델 8개와 그 아래가 전부 영향을 받습니다.

프로시저 문법을 dbt 로 옮기면서 규칙을 몇 개 정했습니다.

프로시저의 임시 테이블을 전부 물리 테이블로 승격하면 DB에 중간 산출물이 그만큼 더 쌓입니다. 그래서 `#temp` 테이블은 ephemeral로 옮겼습니다. 프로시저 단계 체인의 `#temp`는 `materialized='ephemeral'`로 두어 컴파일 시 CTE로 인라인되게 했습니다. 물리 테이블 없이 단계 분해는 그대로 유지됩니다. 최종 분포는 table 53, ephemeral 9였고 나머지 2개는 설정 파일 문제로 기본값 view에 떨어져 있었습니다. 예외가 하나 있었습니다. ephemeral 모델을 `LEFT JOIN` 우측에 놓았더니 에러가 나서 그 모델만 table로 우회했습니다. CTE로 한 번 감싸면 됐을 거라는 짐작은 있지만 당시엔 확인하지 못했습니다.

`@campmonth` 파라미터는 `vars`로 올렸습니다. 프로시저는 캠페인 대상월을 파라미터로 받아 계약 경과월을 계산하고 휴지기(재접촉 유예 기간)에 걸리는 고객을 걸러냈습니다. 이 값을 `dbt_project.yml`의 `vars`로 빼고 모델에서는 `var('campmonth')`로 읽게 했습니다. 매월 값 하나만 바꿔 같은 프로젝트를 다시 돌리면 됩니다. 프로시저마다 반복되던 "캠페인월 ±N개월" 계산은 매크로 하나로 뽑아 25곳에서 재사용했습니다. 스냅샷 테이블의 최신 기준월을 `run_query`로 조회해 기본값으로 쓰는 매크로도 하나 뒀습니다.

캠페인마다 필요한 모델 집합이 달랐습니다. 그래서 모델 이름 대신 태그로 실행 단위를 잡았습니다. 대부분의 모델(58/64)에 그레인(고객/증권/피보험자 단위), 역할(모수/제외조건), 캠페인 세 축의 태그를 붙였습니다. `dbt run --select tag:...`로 캠페인 단위 부분 실행이 됩니다.

```bash
# 캠페인 대상월만 바꿔 특정 캠페인 모델군만 실행 (예시)
dbt run --select tag:campaign_a --vars '{"campmonth": "202401"}'
```

하드코딩 테이블명은 `ref()`와 `source()`로 바꿨습니다. 프로시저의 3-part/4-part 테이블명을 프로젝트 내 모델이면 `ref()`로, 외부 테이블이면 `source()`로 바꿉니다. 
단계 하나를 예로 대략 아래와 같습니다. 

```sql
-- 이관 전: 프로시저의 한 단계 (단순화)
SELECT c.CustomerKey, MAX(p.PolicyNo) AS RecentPolicyNo
INTO #recent_policy
FROM dw.PolicyLatest p
JOIN #valid_customer c ON c.CustomerKey = p.CustomerKey
GROUP BY c.CustomerKey;
```

```sql
-- 이관 후: models/subtable/recent_policy.sql (단순화)
{{ config(materialized='table') }}

SELECT c.CustomerKey, MAX(p.PolicyNo) AS RecentPolicyNo
FROM {{ source('dw', 'PolicyLatest') }} p
JOIN {{ ref('valid_customer') }} c ON c.CustomerKey = p.CustomerKey
GROUP BY c.CustomerKey
```

그 밖에 최상류 스냅샷 뷰에는 `loaded_at_field`를 걸어 source freshness(warn 1일 / error 5일)를 켰습니다.  `schema.yml`에는 청구 플래그의 판정 임계값, 정책 변경 이력(날짜와 사유), 상태코드 목록 같은 도메인 지식을 적었습니다. 코드만 읽어서는 복원할 수 없는 것들입니다. 모델별 실행 시간을 로그 테이블에 남기는 pre/post hook도 설정했는데 이건 실제로 한 번도 적용되지 않았습니다. 이유는 아래에 있습니다.

## 왜 Dagster였나

보험사라 DB는 폐쇄망에 있었고 접근하려면 전산센터 단말기에서 가상 윈도우에 붙어야 했습니다. 인터넷이 안 되니 파이썬 라이브러리 하나도 밖에서 받아 물리적으로 반입해야 했습니다.  SSIS 같은 배치 도구도 없어서 처음엔 파이썬 스크립트를 윈도우 작업 스케줄러에 걸었고 나중에 APScheduler로 옮겼습니다. 실행 이력은 로그 파일에만 남았습니다. 어느 스크립트가 언제 실패했는지 보려면 파일을 직접 뒤져야 했습니다.

Airflow를 쓰고 싶었지만 할당된 자원은 그 가상 윈도우 하나뿐이었습니다. Hyper-V가 막혀 있어 그 위에 리눅스 VM이나 도커를 올릴 수 없었습니다. 별도 리눅스 리소스를 받는 것도 어려웠습니다. Dagster는 순수 파이썬이라 `pip install dagster dagster-webserver` 뒤 `dagster dev` 한 줄로 웹 UI가 떴습니다. 이 환경에서 UI로 배치를 관리할 수 있는 도구는 사실상 이것뿐이었습니다.

다만 환경은 진입 조건이었고 계속 쓴 이유는 워크플로우 모델입니다. Airflow류는 "무엇을 실행할지"(task)를 나열하고 Dagster는 "무엇이 만들어지는지"(asset)를 선언합니다. dbt의 모델이 정확히 후자입니다. 모델 하나가 테이블 하나이고 `ref()`가 계보를 만듭니다. Dagster는 dbt 프로젝트를 읽어 모델을 asset으로 그대로 흡수합니다. dbt의 리니지가 asset 그래프로, dbt의 `group`과 `tags`가 UI의 그룹 뷰로 이어집니다. 운영에서는 어떤 마트가 언제 갱신됐는지를 UI에서 테이블 단위로 바로 볼 수 있게 됐습니다. 장애가 나면 그래프에서 upsteram 테이블을 따라 올라가는 것으로 원인 추적이 끝났습니다. 품질 이슈가 생기면 문제 asset과 그 downstream만 특정할 수 있었습니다. 변경된 자산만 골라 다시 materialize할 수 있었습니다.

트레이드오프도 있었습니다. Airflow에 비해 커뮤니티와 한국어 자료도 적습니다. 그리하여 공식 docs 를 통해 학습을 했습니다. asset·materialization·partition 같은 개념이 낯설어 초기 진입이 느렸습니다. 
Airflow 와의 개념 비교는 [[Dagster의 자산 중심 워크플로우]]와 [[Airflow 3와 Dagster 비교]]에 따로 정리했습니다.

---

상세 기록: [[고객마트 dbt 구조 진단]] (폴더·네이밍·SQL 품질·이슈 전수) · [[고객마트 리니지 분석]] (의존성 그래프 정량 분석, 중복 SQL 군) · [[고객마트 개선 로드맵]] (staging/intermediate/marts 재편과 무중단 마이그레이션 순서) · [[고객마트 Dagster 통합]] (dbt 실전 기능 5종의 설계와 실제) · [[고객마트 로컬 복원]] (DuckDB로 재현해 위 결함들을 실제로 고친 기록)

관련 학습 프로젝트: [[Projects/dbt-mini-mart/index|dbt-mini-mart]] — 공개 데이터셋으로 표준 4계층 모델링을 연습한 프로젝트. 이번 실무 구조와 대비해 읽으면 좋습니다.
